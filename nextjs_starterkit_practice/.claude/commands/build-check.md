# 빌드 및 린트 검사

코드 품질 검사를 한 번에 실행합니다.

## 실행 단계

### 1. ESLint 검사

```bash
npm run lint
```

린트 오류가 있으면:
- 오류 내용 분석
- 자동 수정 가능한 항목 확인 (`npm run lint -- --fix`)
- 수동 수정 필요한 항목 안내

### 2. TypeScript 타입 검사

```bash
npx tsc --noEmit
```

타입 오류가 있으면:
- 오류 위치와 내용 분석
- 수정 방법 제안

### 3. 프로덕션 빌드

```bash
npm run build
```

빌드 오류가 있으면:
- 오류 원인 분석
- 해결 방법 제안

## 결과 요약

모든 검사 완료 후:
- ✅ 통과한 검사 목록
- ❌ 실패한 검사와 오류 요약
- 🔧 권장 수정 사항

## 지침

1. 순서대로 lint → 타입 검사 → build 실행
2. 각 단계에서 오류 발견 시 즉시 분석
3. 오류 수정 제안 (자동 수정 vs 수동 수정 구분)
4. 모든 검사 통과 시 성공 메시지 출력
5. 심각한 오류는 즉시 수정 제안
